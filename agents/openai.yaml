interface:
  display_name: "OpenAI-Compatible Image Generation"
  short_description: "Generate, edit, batch, and QA images via compatible APIs."
  default_prompt: "Use $openai-compatible-imagegen to create or edit images through the configured OpenAI-compatible API."
