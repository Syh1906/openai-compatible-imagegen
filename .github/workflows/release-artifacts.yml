name: Publish release

on:
  workflow_dispatch:
    inputs:
      release_ref:
        description: Annotated version tag to publish, for example v1.0.2
        required: true
        type: string

permissions:
  contents: write

concurrency:
  group: release-artifacts-${{ inputs.release_ref }}
  cancel-in-progress: false

jobs:
  validate_release:
    runs-on: ubuntu-latest
    outputs:
      release_tag: ${{ steps.release.outputs.release_tag }}
    steps:
      - name: Check out the requested ref
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.release_ref }}
          fetch-depth: 0

      - name: Validate the annotated release tag
        id: release
        shell: bash
        env:
          RELEASE_TAG: ${{ inputs.release_ref }}
        run: |
          set -euo pipefail
          manifest_version="$(node -p "JSON.parse(require('node:fs').readFileSync('.codex-plugin/plugin.json', 'utf8')).version")"
          expected_tag="v${manifest_version}"
          if [[ "$RELEASE_TAG" != "$expected_tag" ]]; then
            echo "Release tag $RELEASE_TAG does not match manifest version $manifest_version." >&2
            exit 1
          fi
          if ! object_type="$(git cat-file -t "refs/tags/$RELEASE_TAG")"; then
            echo "Release tag $RELEASE_TAG does not exist in the checked-out repository." >&2
            exit 1
          fi
          if [[ "$object_type" != "tag" ]]; then
            echo "Release tag $RELEASE_TAG must be annotated." >&2
            exit 1
          fi
          echo "release_tag=$RELEASE_TAG" >> "$GITHUB_OUTPUT"

      - name: Validate versioned release notes
        shell: bash
        env:
          RELEASE_TAG: ${{ steps.release.outputs.release_tag }}
        run: node scripts/validate-release-notes.mjs --tag "$RELEASE_TAG"

  candidate:
    needs: validate_release
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: windows
            os: windows-latest
            artifact: candidate-windows
          - name: linux
            os: ubuntu-latest
            artifact: candidate-linux
          - name: macos
            os: macos-latest
            artifact: candidate-macos
    runs-on: ${{ matrix.os }}
    steps:
      - name: Check out the requested ref
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.release_ref }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install Node dependencies
        run: npm ci

      - name: Build the Plugin
        run: npm run build

      - name: Run tests
        run: npm test

      - name: Check the Plugin package
        run: npm run check

      - name: Compile Python sources
        run: python -m compileall -q scripts

      - name: Check diffs
        run: git diff --check

      - name: Build the release candidate
        run: node scripts/build-release-artifacts.mjs --source-root . --output-directory release

      - name: Upload the release candidate
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: release/*
          if-no-files-found: error
          retention-days: 14

  compare_candidates:
    needs: candidate
    runs-on: ubuntu-latest
    steps:
      - name: Check out the requested ref
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.release_ref }}

      - name: Download the Windows candidate
        uses: actions/download-artifact@v4
        with:
          name: candidate-windows
          path: candidates/windows

      - name: Download the Linux candidate
        uses: actions/download-artifact@v4
        with:
          name: candidate-linux
          path: candidates/linux

      - name: Download the macOS candidate
        uses: actions/download-artifact@v4
        with:
          name: candidate-macos
          path: candidates/macos

      - name: Compare Windows and Linux candidates
        run: >-
          node scripts/compare-release-artifacts.mjs
          --left candidates/windows
          --right candidates/linux

      - name: Compare Windows and macOS candidates
        run: >-
          node scripts/compare-release-artifacts.mjs
          --left candidates/windows
          --right candidates/macos

  publish:
    needs: [validate_release, compare_candidates]
    runs-on: ubuntu-latest
    environment: release
    steps:
      - name: Check out the requested ref
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.release_ref }}

      - name: Download the verified Windows release candidate
        uses: actions/download-artifact@v4
        with:
          name: candidate-windows
          path: candidates/windows

      - name: Create the GitHub Release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ needs.validate_release.outputs.release_tag }}
          RELEASE_NOTES_PATH: .github/release-notes/${{ inputs.release_ref }}.md
        run: >-
          gh release create "$RELEASE_TAG" candidates/windows/*
          --verify-tag
          --title "$RELEASE_TAG"
          --notes-file "$RELEASE_NOTES_PATH"
